---
sidebar_position: 3
---

# Выделение петель

В редакторе можно выделять петли, чтобы редактировать их не по одной.

Доступны два инструмента выделения:

- **Прямоугольное выделение**
- **Волшебная палочка**

Кроме того, в контекстном меню есть быстрые команды выделения рядов, столбцов и всех петель.

---

## Прямоугольное выделение

Иконка инструмента:

<img
  src={require('/img/knitting/rectangleSelect.png').default}
  srcSet={require('/img/knitting/rectangleSelect.png').default + " 2x"}
  alt="Инструмент прямоугольного выделения"
/>

Этот инструмент выделяет петли внутри выбранной области.

Чтобы открыть настройки, нажмите и удерживайте иконку:

<img
  src={require('/img/knitting/rectangleSelectSettings.png').default}
  srcSet={require('/img/knitting/rectangleSelectSettings.png').default + " 2x"}
  alt="Настройки прямоугольного выделения"
/>

### Режимы комбинирования выделения

Оба инструмента выделения поддерживают одинаковые режимы комбинирования:

- **Заменить выделение**  
- **Добавить к выделению**  
- **Оставить пересечение**  
- **Вычесть из выделения**

---

## Волшебная палочка

Иконка инструмента:

<img
  src={require('/img/knitting/magic.png').default}
  srcSet={require('/img/knitting/magic.png').default + " 2x"}
  alt="Инструмент Волшебная палочка"
/>

Волшебная палочка выделяет **все петли, похожие на ту, по которой вы нажали**.

Волшебная палочка может учитывать:

- **Пряжу**
- **Плотность**
- **Значение петли (тип петли)**

Вы можете включить один или несколько признаков — выделятся только те петли, которые совпадают по выбранным параметрам.

<img
  src={require('/img/knitting/magicSettingsHowToChoose.png').default}
  srcSet={require('/img/knitting/magicSettingsHowToChoose.png').default + " 2x"}
  alt="Как выбрать признаки волшебной палочки"
/>

У волшебной палочки также есть режимы наложения

<img
  src={require('/img/knitting/magicSettings.png').default}
  srcSet={require('/img/knitting/magicSettings.png').default + " 2x"}
  alt="Настройки волшебной палочки"
/>
---

## Пример выделения

Так отображаются выделенные петли:

<img
  src={require('/img/knitting/selectedStitches.png').default}
  srcSet={require('/img/knitting/selectedStitches.png').default + " 2x"}
  alt="Пример выделенных петель"
/>

---

## Выделение через контекстное меню

Правый клик по полотну открывает контекстное меню с дополнительными командами выделения:

<img
  src={require('/img/knitting/contextMenuSelect.png').default}
  srcSet={require('/img/knitting/contextMenuSelect.png').default + " 2x"}
  alt="Контекстное меню с командами выделения"
/>

Доступные команды:

- **Снять выделение**
- **Выделить ряд**
- **Выделить столбец**
- **Выделить всё**

---

## Перемещение выделения

Выделение можно двигать стрелками на клавиатуре. Удобно, когда надо на конкретное число петель или рядов сдвинуться.

---

## Как снять выделение

Выделение можно убрать несколькими способами:

- **Клавиатура:** `Ctrl`/`Cmd` + `D` — снять выделение.
- **Контекстное меню:** пункт **«Снять выделение»**.
- **Клик по пустому месту** на полотне — выделение сбрасывается автоматически.
